# マウス操作を有効化
set -g mouse on

# Ctrl+Enter など、通常の terminfo では Enter と区別できないキーを
# pane 内の TUI アプリへ CSI-u 形式で渡す。
set -as terminal-features ',*:extkeys'
set -g extended-keys on
set -g extended-keys-format csi-u
